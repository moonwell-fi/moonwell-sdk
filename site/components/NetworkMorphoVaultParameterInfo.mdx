:::info
When a type is passed to the function, the parameters change, making `network` and `vault` required instead of `chainId` and `vaultAddress`. The vault field is restricted to the vaults of the chain specified as the parameter.
:::
