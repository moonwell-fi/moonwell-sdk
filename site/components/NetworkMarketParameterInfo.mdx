:::info
When a type is passed to the function, the parameters change, making `network` and `market` required instead of `chainId` and `marketAddress`. The market field is restricted to the markets of the chain specified as the parameter.
:::