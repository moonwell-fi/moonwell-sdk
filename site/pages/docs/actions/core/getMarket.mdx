import NetworkMarketInfo from '../../../../components/NetworkMarketParameterInfo.mdx';
import { LiveExample } from '../../../../components/LiveExample';
import { moonwellClient } from '../../../../snippets/moonClient';

# getMarket

Returns the market.

## Usage

:::code-group

```ts twoslash [example.ts]
import { moonwellClient } from './client'

const market = await moonwellClient.getMarket({ // [!code focus:4]
  chainId: 8453,
  marketAddress: "0x628ff693426583D9a7FB391E54366292F509D457",
});
```

```ts twoslash [client.ts] filename="client.ts"
// [!include ~/snippets/moonClient.ts]
```

:::


## Returns

:::details[Example]

<LiveExample promise={
  moonwellClient.getMarket({
  chainId: 8453,
    marketAddress: "0x628ff693426583D9a7FB391E54366292F509D457",
  })
} />

{/* ```ts twoslash
import { Market } from "@moonwell-fi/moonwell-sdk/types/market";
import { Amount } from "@moonwell-fi/moonwell-sdk/common";
// ---cut---

const exampleResult: Market = {
  marketKey: '',
  chainId: 0,
  deprecated: false,
  mintPaused: false,
  borrowPaused: false,
  seizePaused: false,
  transferPaused: false,
  marketToken: {
    address: `0x00000000000000000`,
    decimals: 0,
    name: '',
    symbol: '',
  },
  underlyingToken: {
    address: `0x00000000000000000`,
    decimals: 0,
    name: '',
    symbol: '',
},
  collateralFactor: 0,
  underlyingPrice: 0,
  supplyCaps: new Amount(0,0),
  supplyCapsUsd: 0,
  borrowCaps: new Amount(0,0),
  borrowCapsUsd: 0,
  totalSupply: new Amount(0,0),
  totalSupplyUsd: 0,
  totalBorrows: new Amount(0,0),
  totalBorrowsUsd: 0,
  totalReserves: new Amount(0,0),
  totalReservesUsd: 0,
  cash: new Amount(0,0),
  exchangeRate: 0,
  reserveFactor: 0,
  baseSupplyApy: 0,
  baseBorrowApy: 0,
  totalSupplyApr: 0,
  totalBorrowApr: 0,
  rewards: [{
    token: {
      address: `0x0000000000000000000000000000000000000000`,
      decimals: 0,
      name: '',
      symbol: '',
    },
    supplyApr: 0,
    borrowApr: 0,
    liquidStakingApr: 0,
  }],
};
``` */}
:::

```
Market | undefined
```

- **Type:** [`Market`](/docs/glossary/types#market)

## Parameters

### chainId

- **Type:** `number`

The chain ID where the market is located.

```ts twoslash
// [!include ~/snippets/moonClient.ts]
// ---cut---
const market = await moonwellClient.getMarket({
  chainId: 8453, // [!code focus]
  marketAddress: "0x628ff693426583D9a7FB391E54366292F509D457",
});
```

### marketAddress

- **Type:** `Address`

The address of the market.

```ts twoslash
// [!include ~/snippets/moonClient.ts]
// ---cut---
const market = await moonwellClient.getMarket({
  chainId: 8453,
  marketAddress: "0x628ff693426583D9a7FB391E54366292F509D457", // [!code focus]
});
```

<NetworkMarketInfo />

### network

- **Type:** `string`

The network where the market is located.

```ts twoslash
// [!include ~/snippets/moonClient.ts]
// ---cut---
import { base } from "@moonwell-fi/moonwell-sdk/environments";

const market = await moonwellClient.getMarket<typeof base>({
  network: "base", // [!code focus]
  market: "MOONWELL_ETH",
});
```

### market

- **Type:** `string`

The name of the market.

```ts twoslash
// [!include ~/snippets/moonClient.ts]
// ---cut---
import { base } from "@moonwell-fi/moonwell-sdk/environments";

const market = await moonwellClient.getMarket<typeof base>({
  network: "base",
  market: "MOONWELL_ETH", // [!code focus]
});
```